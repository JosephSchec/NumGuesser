# First attempt at GoLang!

### Simple number guesser game with Go's built in scanner

Things I learned:

- `go mod init` should be from Project Level
- how to import packages in Go
- How to use basic loop, declerations and pointers in Go
